@model IEnumerable<P11WebAPI.P11DbModel.T_Match_PlayerMappingTable>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.M_MatchInfoMaters.MatchLocation)
        </th>
       <th>
            @Html.DisplayNameFor(model => model.M_PlayerInfoMaster.PlayerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlayerPosition)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.M_MatchInfoMaters.MatchLocation) - @Html.DisplayFor(modelItem => item.M_MatchInfoMaters.M_TeamInfoMaster.TeamCode) Vs @Html.DisplayFor(modelItem => item.M_MatchInfoMaters.M_TeamInfoMaster1.TeamCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.M_PlayerInfoMaster.PlayerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlayerPosition)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.PlayerMatchID }) |
            @Html.ActionLink("Details", "Details", new { id = item.PlayerMatchID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.PlayerMatchID })
        </td>
    </tr>
}

</table>
